# Default retention/storage directory of a corestreamd run from the repo root.
corestreamd-data/

# Benchmark output.
*.csv

# Binaries from `go build ./cmd/...` in the repo root — 39MB each; one
# `git add .` would bake them into history forever.
/corestreamd
/benchrunner
